// helper util for cases where a hoc is conditionally applied
// just pass through the component without wrapping in a hoc
const withPassthrough = Component => Component;

export default withPassthrough;
